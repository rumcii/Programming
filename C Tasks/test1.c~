#include <stdio.h>
#include <string.h>

struct Node {
    int data;
    struct Node* link;
};

struct Node* head;

main()
{
/*    Node* A;*/
/*    A = NULL;*/
/*    */
    Node *temp = (Node*)malloc(sizeof(Node));
}
